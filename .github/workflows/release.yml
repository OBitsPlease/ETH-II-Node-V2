name: Build and Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.22'

      - name: Build stratum.exe
        run: |
          cd stratum
          go build -o ../stratum.exe .

      - name: Clone and build ethii node
        run: |
          if ("${{ secrets.NODE_REPO_TOKEN }}" -eq "") {
            Write-Error "Missing secret NODE_REPO_TOKEN. Add a PAT with read access to OBitsPlease/ethii-node."
            exit 1
          }
          git clone https://x-access-token:${{ secrets.NODE_REPO_TOKEN }}@github.com/OBitsPlease/ethii-node.git ethii-src
          cd ethii-src
          go build -o ../ethii.exe ./cmd/geth

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Build wallet installer
        run: |
          cd wallet
          npm ci
          npx electron-builder --win

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            wallet/dist/*.exe
            stratum.exe
            ethii.exe
            watchdog-autopilot.ps1
            launch-stratum.ps1
            README.md
