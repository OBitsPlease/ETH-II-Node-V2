# ETH-II Node Access Request Template

Fill this out and **DM it to `@bitspleaseyt.skr` on Discord**, or open a
GitHub issue on this repo titled `Access request`.

- Contact handle:
- Purpose: peer-support node or pool operator
- Pool name (if pool operator — shown in new-pool announcements):
- Pool website / Discord link (optional, shown in announcements):
- Expected host type: VPS / home PC / dedicated server
- Region/timezone:
- Public IP (if known):
- I agree to run only the approved ETH-II binaries and report first startup identity values.
